# Dependencies
node_modules
.pnp
.pnp.js

# Build output
dist
dist-ssr
*.local

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment
.env
.env.local
.env.*.local

# Netlify
.netlify
deno.lock

# Build / dev artifacts
dev.log
*.tsbuildinfo
coverage

# Local tooling state — se ignora todo lo personal de Claude Code (worktrees,
# launch.json, settings)…
.claude/*
# …excepto las skills versionadas del repo (proceso compartido con el equipo/Codex).
!.claude/skills/
# Espejo local de esas mismas skills que crea el tooling de agentes: la copia
# versionada es .claude/skills/, y dos copias divergen (la de acá venía
# reformateada por Prettier).
.agents/

# Playwright
test-results/
playwright-report/
playwright/.cache/
